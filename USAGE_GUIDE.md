# 사용자 가이드

스마트 광고 AR 시스템 사용법

## 빠른 시작

### 1단계: 서버 실행
```bash
python web_app.py
```

서버가 시작되면 출력되는 주소를 확인하세요:
```
🚀 Flask 서버 시작...
 * Running on https://127.0.0.1:5000
 * Running on https://192.168.x.x:5000
```

### 2단계: PC에서 페르소나 설정
1. PC 브라우저에서 `https://localhost:5000/admin` 접속
2. 원하는 페르소나 선택 또는 커스텀 설정
3. "💾 저장 및 적용" 클릭

### 3단계: 스마트폰에서 광고 촬영
1. 스마트폰 브라우저에서 `https://192.168.x.x:5000/` 접속
2. 상단에 페르소나 정보 확인
3. 카메라 시작 후 광고판 촬영
4. 개인화된 오버레이 확인

---

## 상세 가이드

### PC 관리 페이지 (`/admin`)

#### 페르소나 선택

6가지 프리셋 페르소나 중 선택:

| 페르소나 | 선호 음료 | 관심사 | 가격 민감도 |
|---------|----------|--------|------------|
| 👩 20대 여성 | 라떼, 바닐라라떼, 카푸치노 | 패션, 뷰티, 카페, SNS | 보통 |
| 👨 20대 남성 | 아메리카노, 콜드브루 | 게임, IT, 전자제품, 운동 | 보통 |
| 👩‍💼 30대 여성 | 라떼, 디카페인 | 육아, 가전제품, 건강, 홈인테리어 | 높음 (프리미엄) |
| 👨‍💼 30대 남성 | 아메리카노, 에스프레소 | 자동차, 투자, 골프, IT | 낮음 (가성비) |
| 👴 시니어 | 아메리카노, 디카페인 | 건강, 여행, 골프, 등산 | 낮음 (가성비) |
| ⚙️ 커스텀 | 직접 설정 | 직접 설정 | 직접 설정 |

#### 커스텀 설정

**☕ 선호 음료**
쉼표로 구분하여 입력:
```
예: 라떼, 아메리카노, 카푸치노
```

**⚠️ 알레르기 성분**
쉼표로 구분하여 입력:
```
예: 우유, 견과류
```

**💡 관심사**
쉼표로 구분하여 입력:
```
예: 운동, 전자제품, 패션
```

**🥗 식이 제한**
- 채식주의자
- 비건

**💰 가격 민감도**
- 낮음 (프리미엄 선호)
- 보통
- 높음 (가성비 중시)

---

### 스마트폰 AR 뷰 (`/`)

#### 화면 구성

```
┌─────────────────────────────────────┐
│ 📱 스마트 광고 AR 시스템              │
├─────────────────────────────────────┤
│ 20대 여성 | 라떼 | 패션 | 뷰티        │  ← 페르소나 정보
│                   ⚙️ 페르소나 설정    │
├─────────────────────────────────────┤
│                                     │
│        [웹캠 화면]                   │
│                                     │
│    💚 추천                            │  ← OCR 기반 오버레이
│    ⚠️ 알레르기 주의                  │
│                                     │
├─────────────────────────────────────┤
│ [카메라 선택 ▼] [시작] [중지] [OCR] │
│ FPS: 30 | OCR: 15 | 오버레이: 2      │
└─────────────────────────────────────┘
```

#### 컨트롤

| 버튼 | 기능 |
|------|------|
| 카메라 시작 | 선택한 카메라 활성화 |
| 카메라 중지 | 카메라 비활성화 |
| OCR ON/OFF | OCR 인식 토글 |
| ⚙️ 페르소나 설정 | 관리 페이지로 이동 |

#### 오버레이 종류

| 아이콘 | 타입 | 설명 | 조건 |
|--------|------|------|------|
| 💚 | 추천 | 선호 메뉴 하이라이트 | 프로필의 선호 음료와 일치 |
| 🔁 | 재구매 | 재구매 제안 | 최근 구매 이력 있음 |
| ⚠️ | 경고 | 알레르기 성분 주의 | 알레르기 성분 감지 |
| 🎁 | 프로모션 | 맞춤형 할인 정보 | 하드코딩 룰 또는 개인화 |
| 🏷️ | 배지 | 특별 혜택 표시 | 신제품, 추가 혜택 등 |

---

## 실시간 동기화

### 동기화 흐름

```
PC 관리 페이지              스마트폰 AR 뷰
      │                          │
      │ 1. 페르소나 변경           │
      ▼                          │
   "저장 및 적용"                  │
      │                          │
      ▼                          │
user_profile.json 업데이트        │
      │                          │
      │◄─────────────────────────┤
      │   2. OCR 요청 (0.6초마다) │
      │                          │
      ├────────────────────────► │
      │   3. 최신 프로필 기반      │
      │      오버레이 생성         ▼
      │                    개인화된 콘텐츠 표시
```

### 동기화 확인
1. PC에서 "20대 여성" 선택 → 저장
2. 스마트폰 페이지 새로고침
3. 상단에 **"20대 여성 | 라떼 | 패션 | 뷰티"** 표시 확인
4. 광고 촬영 시 "라떼"에 💚 추천 배지 표시

---

## 테스트 시나리오

### 시나리오 1: 카페 메뉴판
**준비물:** 카페 메뉴판 또는 메뉴 이미지

1. PC에서 "20대 여성" 페르소나 선택
   - 선호 음료: 라떼, 바닐라라떼, 카푸치노
2. 스마트폰으로 메뉴판 촬영
3. **예상 결과:**
   - "라떼" 메뉴에 💚 추천 표시
   - "카푸치노"에도 💚 추천 표시

### 시나리오 2: 알레르기 경고
**준비물:** 식품 성분표 또는 메뉴

1. PC에서 알레르기 설정
   - 알레르기: 우유, 견과류
2. 스마트폰으로 메뉴 촬영
3. **예상 결과:**
   - "우유" 또는 "견과류" 포함 메뉴에 ⚠️ 경고 표시

### 시나리오 3: 하드코딩 광고

#### LG Electronics
- **인식 키워드:** "LG", "Electronics"
- **예상 오버레이:**
  - 🔌 LG 신제품 출시 특가! 최대 30% 할인
  - 5년 무상 A/S

#### BALANCE LAB
- **인식 키워드:** "Balance", "LAB", "근골격", "운동"
- **예상 오버레이:**
  - 💪 첫 방문 고객 1개월 무료 체험!
  - 🏋️ PT 3개월 등록 시 1개월 추가 증정

---

## 문제 해결

### Q: 스마트폰에서 "카메라 접근 권한 없음"
**A:** HTTPS로 접속했는지 확인
```
❌ http://192.168.x.x:5000
✅ https://192.168.x.x:5000
```

### Q: "저장 실패" 오류
**A:**
1. Flask 서버 재시작: `Ctrl+C` → `python web_app.py`
2. 브라우저 새로고침

### Q: OCR이 텍스트를 인식하지 못함
**A:**
- 광고판에 가까이 다가가기
- 조명 밝게
- 텍스트 선명하게 초점 맞추기

### Q: 페르소나 변경이 반영되지 않음
**A:**
- 스마트폰 페이지 새로고침 (F5)
- `config/user_profile.json` 파일 확인

### Q: 오버레이가 표시되지 않음
**A:**
1. OCR이 텍스트를 인식했는지 확인 (디버그 패널)
2. 페르소나 설정이 저장되었는지 확인
3. 브라우저 콘솔에서 에러 확인 (F12)

---

## 고급 사용법

### 프로필 JSON 직접 편집
`config/user_profile.json` 파일을 직접 수정:

```json
{
  "user_id": "user001",
  "preferences": {
    "coffee_type": ["라떼", "아메리카노"],
    "dietary": {
      "allergies": ["우유", "견과류"],
      "vegetarian": false,
      "vegan": false
    },
    "price_sensitivity": "medium",
    "interests": ["패션", "IT", "운동"]
  },
  "history": {
    "recent_purchases": ["아메리카노", "라떼"],
    "favorite_items": ["바닐라라떼"]
  },
  "personalization_level": "high"
}
```

### 카메라 선택
- Camo 카메라는 자동으로 선택됨
- 드롭다운에서 다른 카메라 수동 선택 가능

### 성능 조절
`static/js/app.js`에서 OCR 간격 조정:
```javascript
this.ocrInterval = 20; // 20프레임마다 OCR (기본값)
```

### 하드코딩 룰 추가
`web_app.py`의 `apply_hardcoded_rules()` 함수 수정:

```python
# 새로운 광고 추가
if '스타벅스' in all_text_lower or 'starbucks' in all_text_lower:
    overlays.append({
        'type': 'promotion',
        'position': 'top-center',
        'content': '☕ 스타벅스 신메뉴 출시!',
        'style': 'coffee',
        'reason': '하드코딩 룰: 스타벅스 감지'
    })
```

---

## 네트워크 설정

### 동일 네트워크 연결 확인

**Windows (PC):**
```bash
ipconfig
```
출력에서 "IPv4 주소" 확인 (예: 192.168.1.100)

**스마트폰:**
- Wi-Fi 설정에서 PC와 동일한 네트워크 연결
- IP 주소가 같은 대역인지 확인 (192.168.1.xxx)

### 방화벽 설정

**Windows 방화벽:**
```
제어판 → Windows Defender 방화벽
→ 고급 설정 → 인바운드 규칙 → 새 규칙
→ 포트 → TCP → 5000 → 허용
```

---

## 다음 단계

- [구현 요약](IMPLEMENTATION_SUMMARY.md): 기술 아키텍처 이해
- [README](README.md): 전체 프로젝트 개요
